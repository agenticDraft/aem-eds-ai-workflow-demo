.hlx/*
coverage/*
logs/*
node_modules/*

# VSC
.DS_Store
*.bak
.idea
.history/
.playwright-mcp/

# Custom AEM Boilerplate
helix-importer-ui
tools

# Ensure these files are tracked
!CLAUDE.md
!AGENTS.md

# Project specific
docs/
.diagram-lock.json

# Secrets — THIS REPO IS PUBLIC. Never remove these lines.
.env
.claude/settings.local.json
.claude/artifacts/

# Delivery-automation run artifacts — ephemeral, instance-local, never committed.
# Spec artifacts under drafts/specs/<ticket>/ are committed on purpose (D4); these are not.
.claude/logs/
.analytics/
run-state.json
progress.md