version: 1

packs:
  platform: eds
  tracker: jira
  scm: github
  design: figma
  browser: playwright

commands:
  lint: "npm run lint"
  test: ""
  build: ""
  serve: "npm run up"

paths:
  spec_dir: ".ai/specs"
  preview: "http://localhost:3000/preview"

limits:
  questions_per_run: 3
  fix_attempts_default: 3
