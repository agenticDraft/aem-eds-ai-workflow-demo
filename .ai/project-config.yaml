version: 1

packs:
  tracker: jira
  scm: github

commands:
  lint: "npm run lint"
  test: ""
  build: ""
  serve: "npm run up"

paths:
  spec_dir: ".ai/specs"
  preview: "http://localhost:3000/preview"
