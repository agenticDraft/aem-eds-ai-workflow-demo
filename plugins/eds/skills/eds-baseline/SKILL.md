---
description: The baseline stage (core contract §4), conditional on a named component being present — captures that component's current rendered state through the browser role. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-baseline

This stage runs only when the fact record names at least one component. It captures the named
component's current rendered state through the `browser` role, so `eds-verify` can compare before
against after, not only after against the design reference. An item naming no component has no
baseline to take, which is why this stage is conditional rather than always-on.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
