---
description: The serve stage (core contract §4) — starts the project's serve command and polls its preview path until it answers. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-serve

This stage always runs. It starts the project's configured `serve` command and polls
`paths.preview` until it answers, so later stages have a live target to render against. A failure
here is transient, per the core's error classes — it does not end the run.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
