---
description: The lint stage (core contract §4) — always runs, up to three fix attempts. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-lint

This stage always runs. It runs the project's configured lint command against the change and
fixes what it finds, up to three attempts before giving up and reporting what remains.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
