---
description: The reserved last stage of every route (core contract §4) — publishes through the scm role and reports back through the tracker role. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-deliver

This is the reserved last stage of every route: publish the change through the `scm` role, then
report back through the `tracker` role.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
