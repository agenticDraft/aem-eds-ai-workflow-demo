---
description: The verify-design stage (core contract §4), conditional on a design reference being present or requested — compares the prototype against the reference through the browser role, with its own internal fix loop. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-verify-design

This stage runs only when the fact record shows a design reference is present or one was
requested. It compares the prototype `eds-prototype` produced against the reference
`eds-extract` retrieved, through the `browser` role. Its fix loop is internal to this stage: at
most two attempts, aborting on no improvement rather than looping indefinitely.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
