---
description: The implement stage (core contract §4) — does the work for a single unit of this project, following the plan eds-plan-gate approved. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-implement

This stage always runs. It carries out the plan `eds-plan-gate` approved, following this
project's own conventions as researched by `eds-conventions`.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
