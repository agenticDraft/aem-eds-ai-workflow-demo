---
description: The extract stage (core contract §4), conditional on a design reference being present or requested — retrieves the design reference and its values through the design role. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-extract

This stage runs only when the fact record shows a design reference is present or one was
requested. It retrieves the design reference and, where the source carries them, its values
through the `design` role. An image-only source yields a comparison target and no values, which
the stages that consume this one's output must handle rather than treat as an empty result.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
