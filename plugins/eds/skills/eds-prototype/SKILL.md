---
description: The prototype stage (core contract §4), conditional on a design reference being present or requested — builds the change so eds-verify-design has something to compare against the reference. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-prototype

This stage runs only when the fact record shows a design reference is present or one was
requested. It builds a working prototype of the requested change so `eds-verify-design` has
something concrete to compare against the design reference `eds-extract` retrieved.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
