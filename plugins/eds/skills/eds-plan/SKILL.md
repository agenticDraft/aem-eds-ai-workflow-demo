---
description: The plan stage (core contract §4) — plans the change before anything is implemented. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-plan

This stage always runs. It turns the fact record, the sanitized specification, and whatever
`eds-conventions` found into a concrete plan for `eds-implement` to follow, checked next by
`eds-plan-gate`.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
