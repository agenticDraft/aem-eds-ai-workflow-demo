---
description: The conventions stage (core contract §4) — researches this project's own conventions before anything is written. Generated by pack scaffolding; replace this stub with a real adapter before running it in a route.
context: fork
---

# eds-conventions

This stage always runs. It researches the project's own conventions — existing patterns, naming,
and structure — so the `implement` stage that follows works with the project rather than against
it.

End your output with the result envelope:

```markdown
## Result
verdict: pass | warn | fail | question
summary: <one sentence, plain prose>
artifacts:
  - <relative path written or updated>
next_action: <short phrase, or "none">
```

This is a generated stub. Replace it with a real adapter that actually implements the above before
using this pack in a route.
