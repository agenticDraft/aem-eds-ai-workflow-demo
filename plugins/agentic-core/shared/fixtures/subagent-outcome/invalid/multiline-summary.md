# Subagent: measure named selectors

Rendered the target page and read the computed values. The summary below wraps onto a second line
instead of staying one sentence — the failure mode this fixture exists to catch.

## Outcome
status: success
summary: Rendered the three named selectors,
  and wrote their computed values.
artifacts:
  - .ai/run-context/measurements.json
next_action: none
