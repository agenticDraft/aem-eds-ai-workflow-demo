# Subagent: measure named selectors

Rendered the target page and read the computed values. The subagent finished cleanly, but the
block below borrows the result envelope's `pass` literal instead of this contract's own
vocabulary — the tier mix-up this fixture exists to catch.

## Outcome
status: pass
summary: Rendered the three named selectors and wrote their computed values.
artifacts:
  - .ai/run-context/measurements.json
next_action: none
