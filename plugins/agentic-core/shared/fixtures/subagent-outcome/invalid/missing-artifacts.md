# Subagent: measure named selectors

Rendered the target page and read the computed values, but the block below omits the `artifacts`
list entirely — the failure mode this fixture exists to catch.

## Outcome
status: success
summary: Rendered the three named selectors and wrote their computed values.
next_action: none
