# Subagent: measure named selectors

Rendered the target page. The requested selector does not exist, but the block below omits the
required `blocker` field — the failure mode this fixture exists to catch.

## Outcome
status: failure
summary: The named selector does not exist in the rendered page.
artifacts: []
next_action: none
