# Stage: intake

Fetched the work item through the `tracker` role and wrote the fact record, but the block below
omits the `artifacts` list entirely — the failure mode this fixture exists to catch.

## Result
verdict: pass
summary: Fetched the work item, sanitized its text, and wrote the fact record.
next_action: none
